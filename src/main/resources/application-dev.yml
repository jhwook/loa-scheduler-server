spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/loa
    username: jhwook
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

server:
  port: 8282
